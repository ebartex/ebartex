import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["imgstatic.ebartex.pl", "via.placeholder.com"], // Dodaj dozwolone domeny
  },
  // Możesz tutaj dodać inne opcje konfiguracji
};

export default nextConfig;
 